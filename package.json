{
  "name": "obsidian-belki-tasks",
  "version": "0.2.3",
  "description": "A minimal Todoist-like task manager using local Markdown files.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "test:vault": "node esbuild.config.mjs production && node scripts/copy-to-test-vault.js",
    "test:vault:reset": "node scripts/reset-test-vault.js && node esbuild.config.mjs production && node scripts/copy-to-test-vault.js"
  },
  "keywords": [
    "obsidian",
    "tasks",
    "todo"
  ],
  "author": "Yasin Aribuga",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.12",
    "esbuild": "^0.23.0",
    "obsidian": "^1.7.2",
    "tslib": "^2.6.3",
    "typescript": "^5.5.3"
  }
}
